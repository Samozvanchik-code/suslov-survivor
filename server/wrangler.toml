name = "suslov-reyting"
main = "worker.js"
compatibility_date = "2026-01-01"

# Хранилище топа. Имя привязки латиницей нарочно: кириллица в аргументах
# консоли Windows превращается в кашу из-за кодировки.
# id подставится командой из README — свой у каждого аккаунта.
[[kv_namespaces]]
binding = "RATING"
id = "СЮДА_ID_ИЗ_КОМАНДЫ_СОЗДАНИЯ"
